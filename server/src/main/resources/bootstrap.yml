spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
      profile: test
  application:
    name: product
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

